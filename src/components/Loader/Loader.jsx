// import PropTypes from 'prop-types';
// import styles from './Loader.module.css';

const Loader = () => {
  return;
};

// Loader.defaultProps = {};
// Loader.propTypes = {};

export default Loader;
